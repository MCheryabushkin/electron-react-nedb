/* eslint-disable prefer-const */
import Datastore from 'nedb';
// import Users from './users';

// eslint-disable-next-line import/no-mutable-exports
// let db = new Datastore({ filename: 'store.db' });
// db.loadDatabase();

// db.find({}, (err, docs) => {
// 	if (!err) {
// 		console.log(docs);
// 	}
// });

// export default db;
